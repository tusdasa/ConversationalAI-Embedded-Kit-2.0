# Example board_devices.yaml configuration for LCD Touch I2C device
# This shows how to integrate the LCD touch I2C device into a board configuration
# All values shown are the actual default values from the device parser

# Example LCD Touch I2C device configuration
- name: lcd_touch          # The name of the device, must be unique
  chip: generic_touch      # Touch chip type (e.g., cst816s, ft5x06, gt911, st1633)
  type: lcd_touch_i2c      # The type of the device, must be unique
  version: default
  dependencies:
    espressif/esp_lcd_touch_generic: "*"  # Component dependency for the touch chip
  config:
    # esp_lcd_panel_io_i2c_config_t fields for I2C communication
    io_i2c_config:
      dev_addr: 0x5a                    # I2C device address (default: 0x5a)
      control_phase_bytes: 1            # Control phase bytes (default: 1)
      dc_bit_offset: 0                  # DC bit offset in control phase (default: 0)
      lcd_cmd_bits: 8                   # Bit-width of LCD command (default: 8)
      lcd_param_bits: 0                 # Bit-width of LCD parameter (default: 0)
      scl_speed_hz: 100000              # I2C SCL frequency (default: 100kHz)
      flags:
        dc_low_on_data: false           # DC level for data transfer (default: false)
        disable_control_phase: true     # Disable control phase for touch (default: true)
      peripherals:
        - name: i2c-0                   # I2C peripheral for touch communication

    # esp_lcd_touch_config_t fields for touch configuration
    touch_config:
      x_max: 320                        # Maximum X coordinate (default: 320)
      y_max: 240                        # Maximum Y coordinate (default: 240)
      rst_gpio_num: -1                  # Reset GPIO (default: -1, GPIO_NUM_NC)
      int_gpio_num: -1                  # Interrupt GPIO (default: -1, GPIO_NUM_NC)
      levels:
        reset: 0                        # Reset pin active level (default: 0)
        interrupt: 0                    # Interrupt pin active level (default: 0)
      flags:
        swap_xy: false                  # Swap X and Y coordinates (default: false)
        mirror_x: false                 # Mirror X coordinates (default: false)
        mirror_y: false                 # Mirror Y coordinates (default: false)
